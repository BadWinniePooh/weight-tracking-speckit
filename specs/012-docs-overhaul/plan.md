# Implementation Plan: Documentation Overhaul

**Branch**: `012-docs-overhaul` | **Date**: 2026-03-17 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/012-docs-overhaul/spec.md`

## Summary

Rewrite `README.md`, create `docs/deployment.md`, `docs/architecture.md`, and `docs/backup.md`, and update `docs/runbook.md` to produce a non-duplicated, single-source-of-truth documentation set for a self-hosted weight tracking application. Each piece of operational or technical information lives in exactly one file; all other files reference it by hyperlink. Diagrams use MermaidJS. No application source code is modified.

## Technical Context

**Language/Version**: Markdown (CommonMark + GitHub Flavored Markdown); MermaidJS for diagrams
**Primary Dependencies**: None — documentation only; MermaidJS renders natively on GitHub
**Storage**: N/A
**Testing**: Manual review against the 6 success criteria in spec.md; link verification
**Target Platform**: GitHub repository viewer (primary); any CommonMark renderer (secondary)
**Project Type**: Documentation
**Performance Goals**: N/A
**Constraints**: Single source of truth per topic; no ASCII diagrams; docs/design-system.md untouched; no application code changes
**Scale/Scope**: 5 documentation files (1 rewrite, 3 new, 1 update)

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle | Status | Notes |
|-----------|--------|-------|
| I. Specification-First | ✅ PASS | spec.md written and validated before any documentation work |
| II. Privacy & Data Ownership | ✅ PASS | No health data involved; documentation describes security configuration (JWT, HTTPS, HttpOnly cookies) accurately |
| III. Test-First | ✅ N/A | Documentation-only feature. No business logic or calculations. Manual review against SC-001 through SC-006 replaces automated tests. Constitution exemption: "non-trivial logic" — documentation prose has no executable logic. |
| IV. Incremental Delivery | ✅ PASS | Three user stories in priority order: self-hoster deploy path (P1), developer architecture (P2), operator runbook (P3). Each file is independently usable after it is written. |
| V. Simplicity (YAGNI) | ✅ PASS | No abstractions, no tooling. Plain Markdown + MermaidJS. No build pipeline added. |

**Gate result**: PASS — proceed to implementation.

## Project Structure

### Documentation (this feature)

```text
specs/012-docs-overhaul/
├── plan.md              # This file
├── research.md          # Baseline audit, content mapping, diagram decisions
└── tasks.md             # Generated by /speckit.tasks
```

### Files Modified or Created

```text
README.md                  # Rewrite: app description, feature summary, quick-start, links
docs/
├── deployment.md          # NEW: prerequisites, env var table, Docker Compose, Traefik/TLS, first-run wizard
├── architecture.md        # NEW: system diagram, data-model diagram, tech stack rationale
├── backup.md              # NEW: create backup, verify backup, restore from backup
└── runbook.md             # UPDATE: remove backup/restore + deployment sections, add health checks, add links
```

**Files explicitly out of scope** (do not modify):

```text
docs/design-system.md      # Out of scope per FR-013
```

## Complexity Tracking

No constitution violations. All five principles satisfied or explicitly N/A for a documentation-only feature.
